module GridTest (tests) where

import Test.Tasty
import Test.Tasty.HUnit

tests = testGroup "Grid module tests"  [
--    isPossibleValuesTestGroup
                                       ] 


-- isPossibleValuesTestGroup = testGroup "isPossibleValues tests" []

-- isPossibleValuesTestPV = testCase "Test on PV constructor" $ assertEqual "Should be true" True actual
--    where
--        actual = isPossibleValues $ PossibleValues
